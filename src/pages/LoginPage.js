
import LoginForm from "../components/login/LoginForm";
const LoginPage = () => {
  
  return (
    <main className="flex-[6] flex items-center justify-center h-screen ">
      <LoginForm/>
    </main>
  );
};

export default LoginPage;
